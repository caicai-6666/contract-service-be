# 项目开发规范

> 本文件适用于在本项目中进行的所有代码开发、修改和功能扩展。

---

## 开发前必读

> **强制要求：** 在开始任何正式开发工作前，必须先阅读 [`description/project.md`](description/project.md)。

该文件说明项目目标、全局边界及文档入口，可用于判断实现位置并定位相关资料。若相关专题文档尚未创建，应先结合现有实现补充必要说明；若关键业务边界仍不明确，再与需求方确认，避免在不了解项目结构的情况下直接开发。

---

## 代码要求

开发实现应遵循以下原则：

1. **注释清晰准确**：在业务规则、关键流程、复杂逻辑、边界处理和设计取舍处添加中文注释；避免为显而易见的代码重复注释。

2. **职责清晰、易于演进**：遵循合适的设计模式和 SOLID 等设计原则，保持职责单一、模块低耦合、接口清晰，并确保代码易于测试与扩展。

3. **抽象适度**：不为套用模式而过度设计；应根据实际复杂度选择恰当的抽象，确保实现简洁、优雅且可维护。

4. **提示词遵循统一规范**：新增或修改模型提示词时，必须遵循[提示词工程规范](description/standard/prompt-engineering.md)，明确任务目标、输入输出、业务规则、失败边界和示例；提示词中的约束必须与代码校验和项目文档保持一致。

5. **工具参数必须精确描述**：每个模型可见的函数工具参数，包括嵌套对象属性和动态生成属性，都必须在实际 JSON Schema 对应字段上具有非空且准确的 `description`；工具总体描述、Python 字段名或 docstring 不能替代字段级说明。具体要求和验证方式遵循[提示词工程规范](description/standard/prompt-engineering.md#25-工具参数描述)。

---

## 功能文档要求

> **同步维护：** 每完成一个功能模块的开发，必须及时在 `description/` 下对应的目录中新建或更新 Markdown 描述文档。

文档至少应包含：模块用途、主要职责、对外接口或使用方式、关键实现与设计决策、依赖关系，以及必要的配置或注意事项。文档应与代码同步维护。

文档的命名、分层、排版和链接维护必须遵循[文档撰写风格手册](description/standard/documentation.md)。

---

## 实验分析日志

> **分析留痕：** 当需求方要求分析某次实验时，除在对话中给出结论外，必须将完整分析追加写入该次实验输出目录下的 `analysis.md`。

实验目录、原始产物和报告结构必须遵循[实验验证规范](description/standard/experiment.md)。

### 日志维护

1. 每个实验运行目录只维护一个 `analysis.md`；文件不存在时创建，已存在时只追加新章节，不覆盖或改写历史分析。

2. 每次分析使用带时区的时间作为二级标题，并至少记录：分析范围、结论摘要、关键证据、原因判断、建议措施和验证状态；实验报告相邻标题之间必须使用 `---` 分隔。

3. 新建文件或追加章节时可按实验特点增减三级小节，但不得省略关键证据和验证状态。

### 证据与表达

1. 关键判断必须链接到同目录中的原始响应、指标、manifest 或校验产物；明确区分“产物可直接观察的事实”和“基于事实的推断”。

2. 不在日志中复制大段原始模型输出；优先使用表格、计数、比例和简短片段呈现证据，保证文档易读。

3. 若同一请求比较多次实验，应在每个被分析的运行目录中追加与该运行相关的结论，并通过相对链接引用其他运行目录。

### 产物边界

> `analysis.md` 是人工分析记录，不由实验运行器自动生成，也不得修改同目录中的原始实验产物。
